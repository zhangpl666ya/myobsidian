# **核心抽象构件**

**1. 势能流形与导引场（Potential Manifold $\Phi$）**

不局限于重力，任何能提供无源能量梯度的物理效应都属于此类：

- **场源载体：** 重力（斜面/跌落）、局部磁场（磁吸对齐）、柔顺弹性力（弹簧回弹）、定向流体（风力/气浮）。
    
- **核心功能：** 在操作空间内建立单向能量梯度 $\nabla \Phi$，保证机械臂一旦失去抓取力，系统状态在无外界主动控制下自发滑向基态。
    

**2. 极限拓扑点位（Topological Boundaries）**

- **初始势阱（全局吸收态 $\mathcal{S}_0$）：** 能量全局最低点。具备强阻尼和确定的位姿约束，用于吸收动能并将物体锁定在已知的初始状态分布中。
    
- **任务目标位（亚稳态/鞍点 $\mathcal{S}^*$）：** 局部的弱平衡区。它需要足够的局部势垒以维持“成功”判定，但稳定边界极窄，轻微侧向外力即可诱发失稳进入下坡流道。
    

**3. 形态对准信道（Morphological Alignment Channel）**

- **功能：** 状态空间降维通道（如导向槽、漏斗、柔顺导向栏）。
    
- **原理：** 利用被动几何干涉，将物体脱手后随机发散的 6-DoF 位姿分布逐步“压缩”，迫使其以唯一的轨迹与姿态滑入初始势阱。
    

**4. 具有对称性破缺的工件（Asymmetric Morphological Design）**

- **设计原则：** 偏心质心、滚动外缘、各向异性摩擦表面。
    
- **功能：** 借助工件形态计算（Morphological Computation）卸除自由度，确保物体平躺或滑行时只能保持唯一的稳定翻滚角，避免尖锐棱角支撑形成的虚假静平衡。
    

**5. 边界仲裁与防死锁看门狗（Verification & Anti-Jamming Watchdog）**

- **就位判断：** 低成本离散传感器（微动开关、光电管、轻量 ROI 检测），作为启动下一轮训练的物理握手信号。
    
- **微扰脱困：** 针对极端小概率的机械自锁（Jamming），配置微小能量注入机制（如偏心微振马达或低频抖动）打破卡死。


# **系统状态集与运作闭环**

这套系统的本质是一个**混杂自治动力学系统（Hybrid Autonomous System）**，状态空间主要包含 4 个离散相：

- **基准就绪态 ($\mathcal{S}_0$)**：物体静止在初始势阱中，自由度被几何约束锁定。
    
- **交互操作态 ($\mathcal{S}_{\text{active}}$)**：机械臂对物体做功，克服势能梯度向目标点推进。
    
- **目标亚稳态 ($\mathcal{S}^*$)**：物体到达高势能目标位，并在微势垒内维持动态平衡。
    
- **无源回流态 ($\mathcal{S}_{\text{return}}$)**：物体脱离机械臂外力，完全由势能场驱动进入物理降维信道。
    

**全流程状态转移逻辑**

**1. 就绪触发（$\mathcal{S}_0 \rightarrow \mathcal{S}_{\text{active}}$）**

- 底部就位传感器（光电/微动）检测到物体在位，向控制节点发送高电平握手信号。
    
- 环境发布环境复位完成信号（`env.step_ready`），算法模型读取当前视觉/本体感知输入，开始执行策略动作。
    

**2. 状态仲裁与分支处理**

在交互操作期间，系统根据实时约束分化出两个单向循环路径：

- __分支 A：成功达标$$\mathcal{S}_{\text{active}} \rightarrow S_{success} \rightarrow \mathcal{S}_{\text{return}}$$
    
    - 物体进入目标位姿容差内，且在无外力支持下维持静止达到设定阈值时间 $T_{\text{hold}}$。
        
    - 控制系统打上 `Success` 奖励标签并记录该 Episode 数据。
        
    - **主动打破平衡：** 机械臂切换为开环控制，向流道方向施加一个微小推力（或气阀喷气），破坏局部亚稳态，工件直接跌入回流信道。
        
- **分支 B：执行失误（$\mathcal{S}_{\text{active}} \rightarrow \mathcal{S}_{\text{return}}$）**
    
    - 机械臂发生脱爪、滑落或超时未完成，判定为 `Failure`。
        
    - 机械臂**立即退回安全操作包络（Home Pose）**，让出物理空间，避免干涉物体的自然下落轨迹。
        
    - 工件在未受外力下，自发沿势能梯度滑入回流信道。
        

**3. 无源收敛与消能（$\mathcal{S}_{\text{return}} \rightarrow \mathcal{S}_0$）**

- 工件在回流信道内受几何干涉约束，高自由度的旋转和弹跳被阻尼材质吸收，位姿逐步收敛，最终滑入初始势阱。
    

**4. 异步看门狗兜底（Jamming Recovery）**

- 机械臂退回 Home 位的同时启动定时看门狗 $T_{\text{timeout}}$。
    
- 若在阈值时间内底部传感器未被触发（发生微小自锁），控制器触发微扰执行器（振动马达通电 0.5s 或气吹扰动），强制系统跳出虚假平衡，直到重新进入 $\mathcal{S}_0$。
    

把构件和闭环流程结合起来，任何具体的应用场景（无论是扶倒立、零件装配、插拔还是堆叠），只要能映射到这几个数学状态与物理信道里，就能直接复用这一套被动复位框架。