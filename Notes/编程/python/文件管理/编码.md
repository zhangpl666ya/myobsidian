>[!tip]
>编码 = 【字符 到 字节】 的翻译规则

- 人类世界：字符（你、a 、中）
- 计算机世界：字节（01010101）

👉编码就是两者转换的字典
乱码大部分情况就是写入时的编码与读取用的编码不同

在读文件时默认是：`utf-8`

>只要是文本文件，永远显式写`encoding='utf-8'

`with open("test.txt","r",encoding='utf8') as f`

📌 好处：
- 跨平台
- 可预期
- 不靠运气

但是二进制模式和编码就没有关系了